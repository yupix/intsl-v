image: yupix/intsl

stages:
  - local_check
  - package_check
  # - server_create

local_check:
    tags:
        - linux
        - archlinux
        - docker
    stage: local_check
    script:
        - export PATH="$(pwd)/usr/bin:$PATH"
        - intsl -V

package_chest:
    tags:
        - linux
        - archlinux
        - docker
    stage: package_check
    script:
        - intsl -V


#server_create_test:
#    tags:
#        - linux
#        - archlinux
#    stage: server_create_test
#    script:
#        - export PATH="$(pwd)/usr/bin:$PATH"
#        - intsl mc make 1 1.15 test -y f
